// lib/import/enrich/openai.ts

/**
 * Placeholder AI-verrijking.
 * Voor nu: geef het artwork ongewijzigd terug.
 * Later vervang je dit door een OpenAI-call (beschrijving, stijl, thema's, etc.).
 */
export async function enrichWithAI(artwork: any): Promise<any> {
  return artwork;
}

export default enrichWithAI;
